// TODO: Replace placeholder config with Firebase project credentials once ready.
// Keep imports local to avoid breaking static export until auth is wired.

export const firebaseApp = null;

export const firebaseAuth = null;
