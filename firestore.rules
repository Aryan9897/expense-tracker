rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Allow read/write only if the request's auth uid matches the document ID (userId)
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
